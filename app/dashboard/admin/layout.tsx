import { ReactNode } from 'react';

export const metadata = {
  title: 'Admin Controls | Elegant System',
  description: 'System administration and user management',
};

export default function AdminLayout({ children }: { children: ReactNode }) {
  return children;
}
